# Django Settings
DEBUG=0
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=*

# Database (using SQLite by default)
# For production, consider using RDS PostgreSQL

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://your-domain.com
